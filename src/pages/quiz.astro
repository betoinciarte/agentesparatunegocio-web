---
import Layout from '../layouts/Layout.astro';
import Quiz from '../components/Quiz';
---

<Layout
  title="Diagnóstico de IA Gratuito — AgentesParaTuNegocio.com"
  description="10 preguntas. 5 minutos. Descubre las 3 áreas donde un sistema de IA personalizado puede tener mayor impacto en tu negocio."
>
  <section style="padding: clamp(60px,10vw,100px) 0;">
    <div class="container">
      <!-- Header -->
      <div style="text-align:center; margin-bottom:48px; max-width:600px; margin-left:auto; margin-right:auto;">
        <span class="badge" style="margin-bottom:20px; display:inline-block;">DIAGNÓSTICO GRATUITO</span>
        <h1 style="font-size:clamp(1.8rem,4.5vw,2.4rem); margin-bottom:16px;">
          ¿Dónde puede la inteligencia artificial darte ventaja competitiva?
        </h1>
        <p style="color:var(--text-secondary); font-size:clamp(0.95rem,2.2vw,1.05rem);">
          10 preguntas · 5 minutos · Reporte instantáneo generado por IA en tiempo real.
        </p>
        <div style="display:flex; justify-content:center; gap:24px; margin-top:20px; flex-wrap:wrap;">
          <span style="display:flex; align-items:center; gap:6px; font-size:13px; color:var(--text-tertiary);">
            <span style="color:var(--coral);">✓</span> Sin compromiso
          </span>
          <span style="display:flex; align-items:center; gap:6px; font-size:13px; color:var(--text-tertiary);">
            <span style="color:var(--coral);">✓</span> Sin tarjeta
          </span>
          <span style="display:flex; align-items:center; gap:6px; font-size:13px; color:var(--text-tertiary);">
            <span style="color:var(--coral);">✓</span> Sin llamada de ventas
          </span>
        </div>
      </div>

      <!-- Quiz Component (React Island) -->
      <div class="card" style="max-width:720px; margin:0 auto;">
        <Quiz client:load />
      </div>
    </div>
  </section>
</Layout>
